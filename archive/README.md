# Belfast TypeScript archive

Frozen TypeScript WebGPU library (`packages/belfast`), WebGL reference (`packages/alfrid`), and browser examples.

This is not the main line. Active development lives in the Rust workspace at the repository root.

```bash
pnpm install
pnpm dev:all
```
