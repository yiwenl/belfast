{
  "name": "belfast-wasm",
  "version": "0.1.0",
  "private": true,
  "description": "WebAssembly facade for the Rust Belfast renderer",
  "type": "module",
  "files": [
    "pkg"
  ],
  "scripts": {
    "build": "wasm-pack build ../../crates/belfast-wasm --target web --out-dir ../../packages/belfast-wasm/pkg",
    "clean": "rm -rf pkg"
  }
}
